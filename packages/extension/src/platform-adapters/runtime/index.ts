export * from './interface'

// Runtime implementations are provided by each environment:
// - ExtensionRuntime: Chrome Extension Service Worker (src/runtime/extension.ts)
// - Other environments can implement RuntimeInterface as needed
